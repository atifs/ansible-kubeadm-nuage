# Check if everything is ok
ansible nuage_k8s_master -i k8s-hosts -m ping
ansible nuage_k8s -i k8s-hosts -m ping -e cluter_name=nuage_k8s

# Run playbook
ansible-playbook -i hosts -e cluster_name=nuage_k8s cluster-create.yml
